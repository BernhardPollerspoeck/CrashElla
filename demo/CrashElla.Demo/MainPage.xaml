<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CrashElla.Demo.MainPage">

    <ScrollView>
        <VerticalStackLayout Padding="30,0"
                             Spacing="25">
            <Image Source="dotnet_bot.png"
                   HeightRequest="185"
                   Aspect="AspectFit" />

            <Label Text="Hello, you!"
                   Style="{StaticResource Headline}"
                   SemanticProperties.HeadingLevel="Level1" />

            <Label Text="Welcome to &#10;the MAUI CrashElla Demo"
                   Style="{StaticResource SubHeadline}" />

            <Button Text="throw a catched Exception."
                    Clicked="OnCatchedClicked"
                    HorizontalOptions="Fill" />

            <Button Text="throw a uncatched Exception"
                    Clicked="OnUncatchedClicked"
                    HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
